import { TwitterClientInterface } from './client';
import { SttTtsPlugin } from './plugins/DeepgramStreamingClient';
import { TwitterClient } from './types';

export {
  type TwitterClient,
  TwitterClientInterface,
  SttTtsPlugin,
  TwitterClientInterface as default,
};
